image: cypress/base:12

stages:
  - build

build:
  stage: build
  script: 
    - cd src
    - npm install 
    - npm run dev &
    - while ! nc -z localhost:3000; do sleep 1; done
    - npm run cypress:run