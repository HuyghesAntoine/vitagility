image: cypress/base:12

stages:
  - build
  - test

build:
  stage: build
  script: 
    - cd src
    - npm install 
    - npm run dev

test:
  stage: test
  script:
    - cd src
    - npm install
    - npm run cypress:run