image: cypress/base:12

stages:
  - build
  - test

build:
  stage: build
  script: 
    - cd src
    - npm install 
    - npm run dev && npm run cypress:run

test:
  stage: test
  script:
    - cd src
    - npm install
    - npm run cypress:run